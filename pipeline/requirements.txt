# One Long Impersonation -- Data Pipeline Dependencies

# Data wrangling
pandas>=2.2.0
numpy>=1.26.0

# Database
# (sqlite3 is stdlib)

# ML models
scikit-learn>=1.5.0       # Wenner Coefficient logistic regression
lifelines>=0.29.0         # Cox proportional hazards survival analysis
networkx>=3.3             # Teacher-student influence graph analysis

# Data ingestion
musicbrainzngs>=0.7.1     # MusicBrainz API client
SPARQLWrapper>=2.0.0      # Wikidata SPARQL queries (demographics)
requests>=2.32.0          # HTTP client for scraping
beautifulsoup4>=4.12.0    # HTML parsing for Rock Hall scraper
lxml>=5.2.0               # Fast HTML/XML parser

# AI generation (Docket briefs)
anthropic>=0.52.0         # Claude API client

# Export
pyyaml>=6.0.0             # YAML for curated data files
